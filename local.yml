- hosts: localhost
  become: no
  pre_tasks:
    - name: update brew
      become: no
      changed_when: False
      homebrew: 
        update_homebrew: True
  tasks:
    - import_tasks: tasks/alacritty-setup.yml
